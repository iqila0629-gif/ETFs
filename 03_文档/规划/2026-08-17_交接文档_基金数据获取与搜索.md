# 交接文档：新项目基金数据获取与搜索（截至 2026-08-17）

> 用途：本交接只覆盖「新基金预测 = 用原始 ETF 预测『新数据』453 支基金」的**基金数据获取/搜索**环节。
> 对接文档：`2026-08-14_交接文档_新项目复用索引.md`（整体项目）、`2026-08-14_数据获取v2规划_多数据库补齐缺失数据.md`（获取规划）。
> 工作区根：`C:\Users\vanessacen\Desktop\新基金预测`

## 一、当前总体进度（453 支总账 `01_数据/eip_master_status.csv`）

| 状态 | 数量 | 说明 |
|---|---:|---|
| 有数据-确认 | 133 | 已核无误 |
| 有数据-待人工核 | 150 | 有面板但需复核份额/名称 |
| 有数据-重试找回(待核) | 54 | 曾误配/找回后待核 |
| 有数据-误配待换 | 21 | 疑似份额/ISIN 配错，待换 |
| 有数据-股票(确认) | 4 | 股票类，非基金 |
| **无数据-待人工下载** | **71** | EODHD API 自动补齐中（本交接核心） |
| 无数据-免费源无解 | 20 | 债券/结构性票据/ETP，免费源无解，已标记不下载 |

## 二、EODHD API 拉取状态（`01_数据/eip_api_download_log.csv`，2026-08-17）

| 状态 | 数量 | 说明 |
|---|---:|---|
| **OK 已落盘** | **18** | `01_数据/api_download/<基金名>.csv`，每支 240-252 行（约 2025-08-18 起一年），列：Date,Open,High,Low,Close,Adjusted_close |
| NOT_FOUND | 9 | 见下方清单，已制定新搜索词策略 |
| PENDING | 26 | 今日 8 个 key 额度用尽（160 次/天），**明早重置后自动续跑** |
| SKIP-结构件 | 18 | 结构化/ETP，不拉 |

**已拉到的 18 支（含 investing 搜不到的）：**
Arbrook G10（IE00BZ60K206）、CT MGMT AMERICAN 3U（LU1864949380）、CTMGMT LUX AMERICAN AU（LU0757425094）、AB American Income INC（LU0091433606 J USD Inc）、AB American Income USD（LU0231611335 S USD Acc）、Amundi Global Agg（LU1437021972）、Artisan（IE00B615T757）、BlackRock BGF×3（LU0425308086 / LU0278466700 / LU0737136415）、Franklin×4（LU0211326755 / LU0211331839 / LU0260871040 / LU0128526570）、FTGF Western Asset GHY（IE00B1BXHP82）、Invesco×2（LU0607513230 / LU1075211273）。

**NOT_FOUND 9 支及已更新的补救策略（`eodhd_batch.py` 的 MAP 已改好，明早自动生效）：**
| 基金 | 新策略 |
|---|---|
| GLOBAL X FDS GBL X FTSE ARGENT USD USD | 直拉 `ARGT.US`（ETF 代码，不再搜索） |
| GLOBAL X FDS GBL X ROBOTICS & ARTIFICIAL USD | 直拉 `BOTZ.US` |
| iShares Macquarie Global Infra USD | `IDIN.LSE`（+备选 US） |
| FTGF GBL BRANDYWNE GLOBAL FIXED INCOME A USD ACC USD | 搜索词改 "Brandywine Global Fixed Income Fund" |
| GUINNESS GAM GLOBAL EQUITY INCOME INC D USD | 搜索词缩短 "Guinness Global Equity Income" |
| ABERDEEN STD - NORTH AMERICAN SMALLER COMPANIES FUND I ACC USD USD | 搜索词改 "abrdn American Growth" |
| THREADNEEDLE LATIN AMERICAN NAV ACC USD | suffix 加 `GB`、`LSE`（已有 ISIN GB0002977949） |
| FRANKLIN TEMPL GLOBAL FOCUS FD A DIS USD | Franklin Global Focus 可能无 UCITS 版本，真无解则标记 |
| FRANKLIN TEMPLETON GLOBAL FOCUS A ACC USD | 同上 |
| CELERITY GLOBAL BALANCED FUND IC A ACC USD | 小众，搜不到则标记 |
| FINCREST GLOBAL EQUITY FUND CLASS A USD | 小众，搜不到则标记 |

## 三、数据源验证结论（2026-08-17 实测）

1. **EODHD = 主力**：`eod/<ISIN>.EUFUND` 直接拉 UCITS 基金日净值，约 1 年历史；免费版 **20 次/天/key**。当前 **8 个 key 全部验证可用**（`01_数据/api_keys.json` 的 `eodhd_list`：原始 `6a81f18e855245.20479217` + 用户补充 7 个）。`fundamentals` 接口免费版被禁（403），别用。
2. **TwelveData 免费版无用**：互惠基金符号需 Grow/Venture 付费计划，仅 `symbol_search` 可作目录参考。key：`653c19025a55429795c929e6054b295a`。
3. **investing.com 覆盖判断**：39 支可下载（32 有 + 7 需核）/ 15 无 / 17 结构件。见 `01_数据/eip_investing_coverage.csv` + `03_文档/规划/2026-08-14_investing搜索词清单_手动下载.md`。
4. **FT Markets / fundinfo / wallstreet-online**：均已实测，免费深度受限（FT 约 1 个月、登录才更多；fundinfo 需注册；wallstreet-online CSV 格式怪且时间受限）。investing 无解的真基金可用这三个补漏，结构化/ETP 只能靠官网。
5. **ProFunds**：用户已确认不用再找。用户已注册 EODHD/TwelveData 并授权用 Microsoft Edge 里的账号（fundinfo/wallstreet-online/FT）。

## 四、用户确认的需求口径（务必延续）

- 基金**只用名字**标识，不要代码/ISIN（交付展示名）。
- XLC **不计入**（默认纯 19 支 ETF）；效果不好再考虑计入。
- 无 2025 年数据的基金**单独拎出来做一个表**，不和其他混在一起。
- 自身信号先排除；门槛先扫描再定，暂时和原来一样。
- 结构件/债券（20 支）免费源无解，直接标记不下载；真找不到就标记，不死磕。

## 五、⚠️ 建模前必须处理的份额核对（本次核查新发现）

1. **FRANKLIN TEMPLETON GLOBAL TOTAL RETURN 两只都拉错份额**：`A MDIS USD USD` 和 `FUN A ACC USD USD` 当前**都**落盘为 `LU0260871040`（基础份额）。EODHD 搜索确认正确份额应为：
   - MDIS → **LU0170475585**（FTIF - Templeton Global Total Return A MDIS USD）
   - ACC → **LU0170475312**（Templeton Global Total Return Fund A(acc)USD）
   - 需重拉这两只（改 MAP 后重跑）。
2. **ALLIANCEBERNSTEIN AMERICAN INCOME USD**：清单名**没有写 ACC/INC**；"INCOME" 是**策略名**（AB American Income Portfolio），不是份额类型。当前 investing 搜索词与 EODHD 都解析到 **S USD Acc（LU0231611335）**——这是假设（合理，因为同名第一条已覆盖 INC），但名字本身未写明，需与上司/用户确认；若确认是 ACC 份额则保留，否则换。另：FT 候选 `IT0005630238` 是 "Mediobanca AllianceBernstein American **Growth** Portfolio"（Growth≠Income），**疑似错误，勿用**。
3. **CTMGMT LUX AMERICAN AU USD ACC USD**：拉到的是 AE 份额（LU0757425094），清单要 AU 份额，需确认（或接受 AE 近似）。
4. **AB American Income INC 那条**：当前落盘 J USD Inc（LU0091433606），正确（INC 份额），可锁定统一。

## 六、investing 搜索词清单 ACC/INC 核查结论（用户提出后已复核）

- 39 支「有/需核」逐支核过份额标记（ACC/INC/DIS/MDIS/CAP 等）与搜索词/备注一致性。
- **唯一看似矛盾的就是 `ALLIANCEBERNSTEIN AMERICAN INCOME USD`**（名字含 INCOME、搜索词是 S USD Acc）——原因见第五节第 2 条：INCOME 是策略名，份额未指定，映射 ACC 是假设，已标注待确认。
- 其余条目（如 FRANKLIN TEMP GLOBAL INC A ACC USD USD、JPMORGAN IF GLOBAL INCOME C DIS HDG USD、PIMCOG GLOBAL ADVIS INCOME PU INC USD 等）里出现的 INCOME/INC 都是**策略名**，份额标记（ACC/DIS/MDIS/INC）在名字里都有写明且与搜索词一致，无冲突。
- 7 支「需核」本身就是要进页核对份额的，维持原样。

## 七、脚本与文件索引

| 路径 | 作用 |
|---|---|
| `02_脚本/新项目/eodhd_batch.py` | **主力**：多 key 轮询、断点续跑。用法 `python eodhd_batch.py --max-calls 140`（每次最多 140 次调用）或 `--only "基金名"`。读 `01_数据/eip_manual_download.csv`（71 支清单） |
| `01_数据/eip_manual_download.csv` | 71 支待下载清单（eodhd_batch 的输入，含 kind/isin/hint） |
| `01_数据/eip_api_download_log.csv` | API 拉取日志（OK/PENDING/NOT_FOUND/SKIP-结构件），PENDING 与 NOT_FOUND 每次运行自动重试 |
| `01_数据/api_download/` | 已拉面板：`<基金名>.csv`（Date,Open,High,Low,Close,Adjusted_close） |
| `01_数据/api_keys.json` | EODHD 8 key + TwelveData key。**敏感，勿提交 git** |
| `01_数据/eip_master_status.csv` | 453 支总账（name/category/status） |
| `01_数据/eip_investing_coverage.csv` | investing 覆盖判断（91 行） |
| `03_文档/规划/2026-08-14_investing搜索词清单_手动下载.md` | investing 手动下载搜索词清单 |
| `03_文档/规划/2026-08-14_人工下载任务清单.md` | 人工下载任务清单（FT/fundinfo/wallstreet） |
| `新数据/EIP AMERICAN.txt`、`EIP GLOBAL.txt`、`EIP INTERNATIONAL.txt` | 原始清单（带序号） |

## 八、下一步（明确）

1. **明早跑** `python eodhd_batch.py --max-calls 140`：拉完 26 PENDING + 重试 9 NOT_FOUND（8 key × 20 次 = 160 额度足够）。
2. 按第五节改 Franklin Total Return 两只的 MAP 后重拉（MDIS→LU0170475585、ACC→LU0170475312），并处理其余待核份额。
3. 复盘 NOT_FOUND 是否真无解（Franklin Global Focus、Celerity、FincRest 大概率真无）。
4. 拉完后合并面板到 `01_数据/新项目_基金价格/`（或独立 `api_download` 目录），更新 master status，进入建模阶段；无 2025 数据的单独出表。
5. 可清理 `02_脚本/新项目/_*.py` 辅助脚本与 `01_数据/__*.txt` 调试文件。

## 九、环境注意事项

- 中文路径经 PowerShell 管道传 Python 会乱码 → **所有 Python 必须写脚本文件再执行**（UTF-8 无 BOM，用 `[System.IO.File]::WriteAllText(path, $content, (New-Object System.Text.UTF8Encoding($false)))`）。
- 网络提权：Python 可执行 `C:\Users\vanessacen\.cache\codex-runtimes\codex-primary-runtime\dependencies\python\python.exe`（已批准 prefix_rule）。
- PowerShell 输出中文设 `$env:PYTHONIOENCODING='utf-8'`；Python 写 CSV 用 `encoding="utf-8-sig"`。
- EODHD 免费额度：20 次/天/key，8 key 共 160 次/天；用完第二天重置。
- `01_数据/api_keys.json` 是敏感文件，勿提交 git。