# v4 阶段规划：原始 ETF 收紧门槛与多策略彩色交付

> 规划日期：2026-08-06
> 阶段定位：在 v3（76/27/19 三版本交付）之后，按上司偏好回归原始 19 支 ETF，提高交易数门槛，补覆盖与低交易数，并把交付从“每基金 1 条策略”升级为“每基金 3-5 条策略 + 日期着色”

## 一、现状基线

### 1.1 当前数据与信号

- 基金：129 支 ProFunds（其中 MPIXX/MPSXX 为货币基金，不预测）。
- ETF：原始 19 支（上司指定）+ 扩展 57 支（拟备份停用）。
- 外部/自身信号：VIX/TNX、基金自身涨跌/连跌反转。
- 当前正式门槛：
  - 全历史：`|Average| >= 0.2%`、交易数 `>= 50`、命中率 `> 55%`
  - 冻结期：`|Average| >= 0.2%`、交易数 `>= 10`、命中率 `>= 55%`
- 当前 v3 版本对照（每基金最佳策略口径）：

| 版本 | ETF | 信号数 | 覆盖基金 | 全历史命中率中位数 | 冻结期命中率中位数 |
|---|---|---:|---:|---:|---:|
| 全部ETF | 76 | 48,854 | 119 | 60.7% | 65.2% |
| 精简ETF | 27 | 7,386 | 119 | 63.5% | 66.7% |
| 原始ETF | 19 | 2,960 | 100 | 59.4% | 62.5% |

### 1.2 已知缺口

- v3 正式名单未覆盖 8 支非货币基金：BRPIX、FDPIX、FDPSX、GVPSX、RDPIX、RDPSX、RTPIX、RTPSX。
- 部分基金冻结期交易数只有 10-30 次，提高门槛后可能掉出正式名单。
- 当前 UOPIX/ULPIX 的最佳策略来自扩展 ETF（如 BND/VCIT/PST/TMV），切换到原始 19 支后这些策略不可用，需要重新选策略。

## 二、五个改动的评估与顺序

建议按以下顺序执行，前一步的结果是后一步的输入：

```text
Phase 0 交付格式审批（与建模并行）
  -> Phase 1 数据范围收窄为原始 19 支 ETF
  -> Phase 2 提高两时期交易数门槛
  -> Phase 3 三信号穷举扫描 + 其他补缺口方法
  -> Phase 4 每基金多策略选择与冲突规则
  -> Phase 5 彩色交付格式生成与校验
```

### Phase 0：交付格式示例审批（先行）

目的：先把“日期着色 + 基金下策略色块”的版式定下来，避免建模全部完成后返工。

已确认的版式决定：

1. 基金名保持一行（每支基金一列），基金名下方每行一条策略并用填充标注；数据区日期格子按生效策略填色，格子内数字为触发日次一交易日实际回报（%）。
2. 保留公司 13 行统计头，统计公式区间按“策略图例行”整体下移。
3. 颜色暂时按基金/ETF 内部调色；策略较少时两个 Sheet 颜色统一。
4. 冻结期和全历史各出一份同版式 Excel。
5. 新增 Sheet3：按公司格式放 19 支 ETF 和 VIX/TNX 外部数据；ETF 名下方标注相关策略，触发策略的日期格子填色，ETF 内部调色。
6. “说明”页不列策略色号，颜色以基金/ETF 名下方的填充为准。

已生成 v2 审批样例：

- `04_结果/示例审批/双基金多策略彩色示例_v2_审批版.xlsx`
- Sheet1“说明”：版式说明，不含策略色号表。
- Sheet2“基金示例”：UOPIX、ULPIX 各 3 条只使用原始 19 支 ETF 的策略，带公司 13 行统计头。
- Sheet3“ETF外部示例”：19 支 ETF + VIX_Chg% + TNX_ChgBp，相关 ETF 下标注策略并在触发日着色。
- 示例策略少，两个 Sheet 颜色统一；同一天多策略触发时暂按 `|全历史 Average|` 最大者着色（近似 R4），最终冲突规则待定。

### Phase 1：数据范围收窄为原始 19 支 ETF

目标：v4 全部建模只使用原始 19 支 ETF + VIX/TNX 外部数据 + 基金自身信号；扩展 57 支只保留备份。

#### Task 1.1：备份扩展 ETF 数据

**文件：**

- Create：`04_结果/备份/扩展ETF_仅备份/`
- Modify：`.gitignore`

**步骤：**

1. 新建备份目录 `04_结果/备份/扩展ETF_仅备份/`。
2. 复制 `04_结果/最新成果/数据/数据_原始/etfs_extended/` 全部文件到备份目录（约 17MB）。
3. 在 `.gitignore` 增加一行 `04_结果/备份/`，除非用户明确要求备份进仓库。
4. 校验：备份目录文件数 = 58（57 支 ETF CSV + 说明.txt），文件大小与原目录一致。

#### Task 1.2：建立 v4 配置与面板脚本

**文件：**

- Create：`02_脚本/adj_close_v4/config.py`
- Create：`02_脚本/adj_close_v4/build_v4_panel.py`
- Create：`01_数据/processed_returns/v4_etf19_panel.csv`
- Create：`01_数据/processed_returns/v4_external_panel.csv`

**`config.py` 内容：**

- `ORIGINAL19`：`SPY QQQ IWM TLT TIP EEM LQD HYG UUP SLV JNK GLD GDX XLV XLU XLE XLF XLK FXY`
- `BACKUP_EXT_DIR`：`04_结果/备份/扩展ETF_仅备份/`
- 输入输出路径：基金面板、ETF 面板、外部数据、v4 中间结果目录。
- 门槛参数集中放在这里，后续 Phase 2/3 共用。

**`build_v4_panel.py` 行为：**

- 读 `01_数据/event_study_inputs/panel_fund_returns_adj.csv`。
- 读 `01_数据/event_study_inputs/panel_etf_returns_adj.csv`。
- 读 `01_数据/event_study_inputs/external_daily.csv`。
- 只保留 `ORIGINAL19` 中的 ETF 列，任何脚本不再加载扩展 ETF。
- 输出 `v4_etf19_panel.csv`（Date + 129 基金 + 19 ETF）。
- 输出 `v4_external_panel.csv`（Date + VIX/TNX 等外部列）。

**验收：**

- `v4_etf19_panel.csv` 的 ETF 列数 = 19，且不含任何扩展 ETF 代码。
- 面板日期范围与 v3 一致。

#### Task 1.3：同步文档口径

**文件：**

- Modify：`README.txt`
- Modify：`04_结果/最新成果/数据/ETF列表说明.txt`
- Modify：`03_文档/数据说明/关键CSV文件用途与结构说明.md`

**行为：**

- 标注“v4 仅使用原始 19 支 ETF，扩展 ETF 已备份停用”。
- 更新三个版本对照表说明：v4 不再保留“全部ETF”和“精简ETF”作为正式交付，只保留原始 19 支版本 + 特批新增 ETF。

#### Task 1.4：“特别特别有用”ETF 准入标准

**文件：**

- Create：`03_文档/数据说明/v4_新增ETF准入标准.md`

**标准（默认不加）：**

1. 必须新增覆盖至少 1 支原始 19 支无法覆盖的基金。
2. 必须让该基金的全历史/冻结期 Average 或命中率相对现有最佳策略有可量化提升。
3. 与现有 ETF 触发重叠低，不能只是平行 ETF。
4. 通过 walk-forward + 冻结期验证，且交易数满足 v4 新门槛。
5. 每次加入需附一页准入说明，由用户书面确认后才进入正式名单。

**Phase 1 验收：**

- v4 脚本全部通过 `config.py` 配置，不再硬编码旧路径 `Desktop\基金预测`。
- 没有任何建模脚本引用扩展 ETF。
- 文档口径与“只用原始 19 支”一致。

### Phase 2：提高两时期交易数门槛

目标：用敏感性表决定两时期交易数新门槛，而不是直接拍板；命中率与 Average 门槛先保持不变。

#### Task 2.1：门槛敏感性扫描

**文件：**

- Create：`02_脚本/adj_close_v4/scan_v4_thresholds.py`
- Create：`04_结果/v4_中间结果/v4_threshold_sensitivity.csv`
- Create：`04_结果/v4_中间结果/v4_19etf_baseline_pass.csv`

**输入：**

- `01_数据/processed_returns/v4_etf19_panel.csv`
- `04_结果/最新成果/中间文档/全部ETF特有/v3_dual_criteria_pass.csv`（只保留条件所需 ETF 全部在原始 19 支内的行）
- `04_结果/最新成果/中间文档/通用/文件/pair_strict_pass.csv`（同上过滤）

**扫描门槛组合：**

```text
全历史交易数：50 / 80 / 100 / 120
冻结期交易数：10 / 15 / 20 / 30
```

**每组合统计：**

- 正式信号数
- 覆盖基金数
- 未覆盖基金清单
- 每基金最佳策略的全历史/冻结期 Average、命中率、交易数中位数

**实现要求：**

- 复用 `evaluate_signal` 的 expanding walk-forward 逻辑，禁止未来信息。
- 先按门槛过滤信号，再选每基金最佳策略；与 v3 口径一致。
- 输出 `v4_threshold_sensitivity.csv`，每行一个门槛组合。
- 推荐默认组合 `全历史 >= 120、冻结期 >= 30` 单独输出为 `v4_19etf_baseline_pass.csv`。

#### Task 2.2：门槛报告与确认

**文件：**

- Create：`04_结果/v4_中间结果/v4_门槛敏感性报告.md`

**内容：**

- 每个门槛组合的覆盖、信号数、中位数对照表。
- 与 v3 原始 19 支基线（旧门槛 2,960 信号 / 100 基金）对比。
- 给出推荐门槛及理由；若推荐值导致覆盖损失过大，列出备选值。
- 用户确认最终门槛后，写入 `config.py` 作为 Phase 3/4 的统一参数。

已确认（2026-08-06）：正式门槛定为全历史 `>= 120`、冻结期 `>= 30`。

**Phase 2 验收：**

- 敏感性表覆盖全部 16 个门槛组合。
- `v4_19etf_baseline_pass.csv` 可按推荐门槛复算。
- 用户已确认正式门槛数值。

### Phase 3：三信号穷举扫描 + 其他补缺口方法

#### 3.1 三信号穷举（新增）

在原始 19 支 ETF 上扩展“三支 ETF 同时满足方向”的条件：

```text
ETF 组合：C(19,3) = 969 组
方向模式：8 种（全涨、全跌、2涨1跌、1涨2跌等，可先全量再筛选）
预测窗口：1 / 2 / 3 日
基金：129 支
约 969 x 8 x 3 x 129 ≈ 300 万个基金-条件组合
```

实现要点：

- 复用 v3 的 `evaluate_signal` 逻辑（expanding walk-forward，禁止未来信息）。
- 用 numpy 向量化，先按事件数预筛（`full_trades >= 门槛`、`frozen_trades >= 门槛`），只输出有潜力的行，不输出全部 300 万行。
- 严格版仍要求未来 N 天全部有数据；与双条件口径保持一致。
- 若“三信号”不是指三支 ETF，而是指“ETF + 外部 + 自身”任意三类信号组合，需先确认条件空间定义。

#### 3.2 其他补缺口方法（按优先级）

1. 外部信号：VIX 变化/水平、TNX 变化、派生特征（已有部分，继续用于 19 ETF 场景）。
2. 基金自身信号：连跌 3 日反转、大涨大跌反转。
3. 高相关 ETF 映射：给每支未覆盖基金找最相关原始 ETF，构造专属条件。
4. 多日窗口：2/3/5 日日均回报，注意与 1 日口径分开标注。
5. 低波动基金：若 0.2% 门槛在物理上不可达，输出“建议接受缺口”或“需新数据源”的证据表。

产出：

- `04_结果/v4_中间结果/v4_triple_scan_stats.csv`
- `04_结果/v4_中间结果/v4_gap_fill_validated.csv`
- `04_结果/v4_中间结果/v4_缺口基金诊断.csv`

执行结果（2026-08-06）：

- 三信号穷举已跑完：969 组合 x 8 方向 x 3 窗口 x 129 基金。
- 数据已完成清洗（基金回报截断 ±50%，截断 70 个异常值；外部 VIX/TNX 变化列已从原始水平重算，修正错位）。
- 通过 39,383 个三信号，覆盖 93 支基金。
- 条件类型扩展通过 19,339 条信号，单独覆盖 88 支基金。
- 三池合并后 61,322 信号 / 98 基金。
- 加 XLC 后覆盖 105；加推荐 3 支扩展 ETF 后覆盖 106。
- 详细结果见 `04_结果/v4_中间结果/v4_phase3_扫描报告.md`。

### Phase 4：每基金多策略选择与冲突规则

目标：每支基金不再只保留 1 条，而是保留不超过 3 条（部分可放宽到 5 条），且整体质量不显著低于该基金的全信号组合。

步骤：

1. 从 v4 通过信号池中，按基金提取候选策略。
2. 对每个基金选策略集合，候选方法：
   - 按 `|全历史 Average|` 排序取 Top-N；
   - 按“互补性”选（不同 ETF 方向、不同窗口、不同触发类型）；
   - 按“交易数补齐”选（先保证覆盖和交易数）。
3. 质量约束建议先量化，例如：
   - 策略集合合并后的 `|Average| >= 全信号组合的 90%`；
   - 命中率不低于全信号组合 3 个百分点；
   - 交易数不低于全信号组合的 80% 或最低门槛；
   - 具体阈值待用户确认。
4. 冲突规则候选并做对比：
   - R4：同日取 `|全历史 Average|` 最大；
   - 多数投票：同日多个策略方向一致才出手，冲突日跳过；
   - 优先级排序：按综合得分（avg/hit/trades）排序取最高；
   - 加权融合：按全历史 Average 加权输出方向。
5. 评估维度：基金级 Average、命中率、交易数、冲突日占比、与全信号组合的差距。

产出：

- `04_结果/v4_中间结果/v4_strategy_selection_report.md`
- `04_结果/v4_中间结果/v4_strategy_mapping.csv`
- `04_结果/v4_中间结果/v4_conflict_rule_comparison.csv`

### Phase 5：彩色交付格式生成与校验

按 Phase 0 审批通过的版式生成正式成果：

- 每支基金最多 3 条策略（放宽后最多 5 条），策略名行填色；
- 数据区日期格子按“当日生效策略”填色，格子内填实际回报（%），未触发留空；
- 保留公司 13 行统计头，公式区间按策略图例行下移；
- 冻结期和全历史各一份，版式相同；
- 每份工作簿含“说明”、“基金示例”（正式版为全部基金）、“ETF外部示例”（19 支 ETF + VIX/TNX）三个 Sheet；
- 颜色按基金/ETF 内部调色；若最终策略数少，两个 Sheet 颜色统一；
- 每基金策略映射、信号逐日明细、覆盖情况同步更新。

校验项：

- Excel 无公式错误；
- 颜色与策略映射一一对应，且不在“说明”页列色号；
- 日期着色与逐日明细交叉核对一致；
- 每基金最佳/策略集合的统计与明细一致；
- 覆盖表与信号池一致。

## 三、关键决策点清单

已确认：

1. 版式：基金名一行，基金名下方每策略一行并用填充标注。
2. 保留公司 13 行统计头。
3. 颜色暂时按基金/ETF 内部调色，策略少时两个 Sheet 统一。
4. 冻结期和全历史各出一份同版式 Excel。
5. 新增 Sheet3：19 支 ETF + VIX/TNX 外部数据，ETF 名下方标注策略并在触发日着色。

执行前仍需确认：

1. 两时期交易数新门槛（建议先看敏感性表）。
2. “三信号”定义：三支 ETF，还是 ETF+外部+自身 的组合。
3. 每基金策略数量：默认 3，哪些基金可放宽到 5。
4. 质量约束如何量化：“不显著低于全信号组合”的具体阈值。
5. 冲突规则：R4 / 多数投票 / 优先级 / 加权。
6. 扩展 ETF 备份是否进入 Git 仓库。

## 四、风险与注意事项

- 提高门槛后覆盖可能明显下降，三信号扫描不一定能完全补回，低波动基金存在物理上限。
- 19 支 ETF 严格口径下覆盖约 100 支（旧门槛），新门槛后可能更少；需要明确“覆盖优先”还是“门槛优先”。
- 多策略会让 Excel 列数/行数显著增加（119 基金 x 3-5 策略），注意文件大小和公式性能。
- 旧脚本大量硬编码 `C:\Users\vanessacen\Desktop\基金预测`，v4 必须先集中配置路径。
- 冻结期只有约 1.5 年，`>= 20` 或 `>= 30` 会淘汰大量信号，需在报告中标注样本风险。

## 五、建议执行顺序小结

```text
1. 审批示例版式（Phase 0，v2 样例已出，待确认）
2. 收窄数据范围 + 脚本可移植（Phase 1）
3. 门槛敏感性表并确认新门槛（Phase 2）
4. 三信号扫描 + 缺口基金补法（Phase 3）
5. 多策略选择 + 冲突规则对比（Phase 4）
6. 彩色交付 + 校验 + 文档更新（Phase 5）
```
